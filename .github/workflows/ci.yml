name: First CI
on: push
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show the commit
        run: echo "Commit: $GITHUB_SHA"
      - name: List the files
        run: ls -la
